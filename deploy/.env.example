# Copy to /opt/prestidata/.env on the box and fill in. Never commit the real one.

# --- Images (GHCR). IMAGE_REPO is ghcr.io/<your-gh-username-or-org> ---
IMAGE_REPO=ghcr.io/CHANGE_ME
IMAGE_TAG=latest

# --- Domain / TLS ---
# Leave DOMAIN BLANK to serve plain HTTP on the box's raw IP (no cert) — good for
# a first smoke test before DNS exists. For a real site, list EVERY hostname you
# point at the box (Caddy auto-provisions HTTPS for each once DNS resolves):
#   DOMAIN=abracadata.me, www.abracadata.me
# Set APEX to the canonical bare domain to 301-redirect www -> apex:
#   APEX=abracadata.me
# ACME_EMAIL is optional (Let's Encrypt expiry notices).
DOMAIN=
APEX=
ACME_EMAIL=

# --- Postgres (on-box container; move to RDS later by changing DATABASE_URL) ---
POSTGRES_USER=prestidata
POSTGRES_PASSWORD=CHANGE_ME_to_a_long_random_string
POSTGRES_DB=prestidata

# --- Bedrock: creds come from the EC2 instance role; only region + models here ---
AWS_REGION=us-east-2
BEDROCK_MODEL=global.anthropic.claude-sonnet-4-6
SUGGEST_MODEL=global.anthropic.claude-haiku-4-5-20251001-v1:0

# --- Sign-in email (SES). Leave blank to log codes instead of emailing.
# Set only after the domain is verified in SES AND production access is granted.
#   MAIL_FROM=Abracadata <login@abracadata.me>
MAIL_FROM=
