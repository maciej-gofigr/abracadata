# Pinned to the exact versions the dev venv + test suite run on, so the built
# image matches the tested environment. (Bump deliberately, then re-run tests.)
fastapi==0.139.2
uvicorn[standard]==0.51.0
anthropic[bedrock]==0.117.0
boto3==1.43.52
SQLAlchemy==2.0.51
psycopg[binary]==3.3.4
